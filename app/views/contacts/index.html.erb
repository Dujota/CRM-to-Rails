<h1>Contacts</h1>

<ul>
  <% @contacts.each do |contact| %>
    <li>
    <%= link_to contact.name, contacts_path(contact)%>
    <br>
    <%= contact.email %>
    <br>
    <%= link_to 'Edit' edit_contact_path(contact) %>
     |
    <%= link_to 'Delete' contact_path(contact), method: :delete, data: {confirm: "Are you sure"} %>

    </li>

  <% end %>
</ul>
